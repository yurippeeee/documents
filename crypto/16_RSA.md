# 16. RSA — 素因数分解の困難性に賭ける

> **公開鍵暗号という発想の衝撃.**
> 1976 年まで、暗号には絶対の前提があった——「送り手と受け手は、事前に秘密の鍵を共有していなければならない」。
>
> ディフィーとヘルマンはこれを覆す論文を書き（[17 章](17_DHと楕円曲線暗号.md)）、
> 1977 年に MIT の Rivest・Shamir・Adleman が具体的な方式を作った。それが **RSA** である。
>
> **鍵を 2 つに分け、片方を全世界に公開する。** それでも安全——
> なぜそんなことが可能なのか、[15 章](15_数論の道具.md)の道具で完全に導出する。

> **この章で使う既出の用語（定義は各リンク先）.**
> $`a \bmod n`$と$`a \equiv b \pmod n`$の 2 つの書き方・$`\gcd`$・逆元$`e^{-1}`$と拡張ユークリッド（[01 章](01_合同算術とユークリッドの互除法.md#合同とは-時計の算数)）、
> 繰り返し二乗法（[01 章 6 節](01_合同算術とユークリッドの互除法.md#繰り返し二乗法-巨大なべき乗を現実的な時間で)）、
> $`\varphi(n)`$（[15 章 1 節](15_数論の道具.md#オイラーの-φ-関数)）、オイラーの定理（[15 章 2 節](15_数論の道具.md#オイラーの定理)）、中国剰余定理（[15 章 3 節](15_数論の道具.md#中国剰余定理-crt)）、
> ミラー・ラビン素数判定（[15 章 6 節](15_数論の道具.md#素数判定-鍵生成に必須)）、$`L_n[\cdot]`$記法（[15 章 5 節](15_数論の道具.md#離散対数問題-dlp)）、
> CCA（[12 章 3 節](12_暗号の基礎概念と古典暗号.md#攻撃モデル-安全-を定義するために)）

## 1. 鍵生成

1. 大きな素数$`p, q`$をランダムに選ぶ（各 1024 ビット以上。乱数を作ってはミラー・ラビン判定にかけ、素数が出るまで繰り返す）
2. $`n = pq`$ を計算（**公開**）
3. $`\varphi(n) = (p-1)(q-1)`$ を計算（**秘密**。$`1`$以上$`n`$以下で$`n`$と互いに素な数の個数）
4. $`\gcd(e, \varphi(n))=1`$ となる$`e`$を選ぶ（**公開**。通常 65537）
5. $`d \equiv e^{-1} \pmod{\varphi(n)}`$、すなわち$`ed \equiv 1 \pmod{\varphi(n)}`$となる$`d`$を計算（**秘密**）。
   手順 4 で$`\gcd(e, \varphi(n)) = 1`$としたのは、この逆元が存在するための条件。求め方は拡張ユークリッド（$`ex + \varphi(n)y = 1`$の$`x`$が$`d`$）

| | 公開鍵 | 秘密鍵 |
|---|---|---|
| 構成 | $`(n, e)`$ | $`(n, d)`$、および$`p,q,\varphi(n)`$ |

> **なぜ$`e = 65537`$なのか.**
> $`65537 = 2^{16}+1 = \texttt{0x10001}`$は素数で、2 進表現が `10000000000000001` と 1 が 2 個だけ。
> 繰り返し二乗法（[01 章](01_合同算術とユークリッドの互除法.md#繰り返し二乗法-巨大なべき乗を現実的な時間で)）では**2 乗 16 回 + 掛け算 1 回**で済み、極めて高速である。
>
> 小さすぎる$`e`$（$`e=3`$など）は危険: 平文が短いと$`m^3 < n`$になり、
> $`\bmod n`$が効かず**普通の 3 乗根を取るだけで解けてしまう**（低指数攻撃）。
> 65537 は「速い」と「安全」のバランス点である。

## 2. 暗号化と復号

$$
\boxed{C = M^e \bmod n, \qquad M = C^d \bmod n}
$$

（$`M < n`$の整数として平文を表現する。）

## 3. なぜ復号できるのか（正しさの証明）

**示すこと**: $`(M^e)^d \equiv M \pmod n`$

### 準備

$`d`$の定義より$`ed \equiv 1 \pmod{\varphi(n)}`$、つまりある整数$`k`$があって

$$
ed = 1 + k\varphi(n)
$$

### 場合 1: $`\gcd(M, n) = 1`$ のとき

オイラーの定理（[15 章](15_数論の道具.md#オイラーの定理)）より$`M^{\varphi(n)} \equiv 1 \pmod n`$。よって

$$
M^{ed} = M^{1+k\varphi(n)} = M \cdot \left(M^{\varphi(n)}\right)^k \equiv M \cdot 1^k = M \pmod n \qquad \checkmark
$$

### 場合 2: $`\gcd(M,n) \neq 1`$ のとき（見落とされがちだが必要）

$`n = pq`$の約数は$`1, p, q, pq`$だけなので、$`\gcd(M, n) \neq 1`$なら$`M`$は$`p`$または$`q`$の倍数。
$`M = 0`$なら$`0^{ed} = 0 = M`$で自明に成立するので、以下$`1 \le M < n`$とし、$`M`$が$`p`$の倍数だとする（$`q`$の倍数でも同様）。

**法$`p`$で見る**: $`M \equiv 0`$なので$`M^{ed} \equiv 0 \equiv M \pmod p`$ ✓

**法$`q`$で見る**: $`M`$が$`q`$の倍数でもあれば$`pq = n`$の倍数となり$`1 \le M < n`$に反するので、$`\gcd(M,q)=1`$。よって
フェルマーの小定理より$`M^{q-1}\equiv1 \pmod q`$。$`\varphi(n)=(p-1)(q-1)`$なので

$$
M^{ed} = M^{1+k(p-1)(q-1)} = M\cdot\left(M^{q-1}\right)^{k(p-1)} \equiv M \cdot 1 = M \pmod q \checkmark
$$

**統合**: $`M^{ed} \equiv M`$が法$`p`$でも法$`q`$でも成立。
$`p,q`$は互いに素なので、中国剰余定理（[15 章](15_数論の道具.md#中国剰余定理-crt)）より$`M^{ed}\equiv M \pmod{pq=n}`$ ∎

> **[15 章](15_数論の道具.md)の 3 つの道具（オイラーの定理・フェルマーの小定理・CRT）が、
> ここで揃って RSA の正しさを支えている.**

### 実例（小さな数で）

$`p=61, q=53`$ → $`n = 3233`$、$`\varphi(n) = 60\times52 = 3120`$
$`e = 17`$（$`\gcd(17,3120)=1`$ ✓）
$`d = 17^{-1} \bmod 3120 = 2753`$（拡張ユークリッド。検算: $`17\times2753 = 46801 = 15\times3120+1`$ ✓）

**暗号化**: $`M = 65`$、$`C = 65^{17} \bmod 3233`$を繰り返し二乗法で計算する。$`17 = 16 + 1 = 10001_2`$なので$`65^{16}`$と$`65^1`$が要る。
2 乗を繰り返して（毎回 3233 で余りを取る）

$$
65^2 \equiv 992,\quad 65^4 \equiv 992^2 \equiv 1232,\quad 65^8 \equiv 1232^2 \equiv 1547,\quad 65^{16} \equiv 1547^2 \equiv 789
$$

よって$`C = 65^{16} \cdot 65 \equiv 789 \times 65 = 51285 \equiv 2790 \pmod{3233}`$（$`51285 = 15 \times 3233 + 2790`$）。

**復号**: $`2790^{2753} \bmod 3233`$。$`2753 = 2048 + 512 + 128 + 64 + 1`$なので、同じく 2 乗を 11 回繰り返して
$`2790^{1} \equiv 2790,\ 2790^{64} \equiv 632,\ 2790^{128} \equiv 1765,\ 2790^{512} \equiv 134,\ 2790^{2048} \equiv 545`$を作り、
この 5 つを掛け合わせると$`65`$に戻る ✓（2 乗 11 回 + 掛け算 4 回。素直に 2752 回掛けるより桁違いに速い）

## 4. 安全性

### 依拠する困難性

**RSA 問題**: $`n, e, C`$から、$`M^e \equiv C \pmod n`$を満たす$`M`$（法$`n`$での「$`e`$乗根」）を求める。

これは**素因数分解問題**と強く関係する:
- $`n`$を素因数分解できれば → $`\varphi(n)`$が分かり → $`d`$が計算でき → 全面的に破れる
- 逆（RSA 問題が解ければ素因数分解できる）は未証明。ただし 40 年以上、素因数分解を経由しない解法は見つかっておらず、
  実務上は「RSA を破る = 素因数分解する」とみなして鍵長を決めている

### 素因数分解の計算量

| アルゴリズム | 計算量 |
|---|---|
| 試し割り | $`O(\sqrt n)`$ |
| Pollard ρ | $`O(n^{1/4})`$ |
| 二次篩 (QS) | $`L_n[1/2]`$（準指数時間） |
| **一般数体篩 (GNFS)** | $`L_n[1/3, 1.923]`$（現在最良） |

$`L_n[\alpha, c] = \exp\!\left(c\,(\ln n)^{\alpha}(\ln\ln n)^{1-\alpha}\right)`$は[15 章](15_数論の道具.md#離散対数問題-dlp)で出た準指数時間の記法。
$`\alpha`$が小さいほど速く、$`\alpha = 1/3`$の GNFS は$`n`$が 100 桁を超えると他を圧倒する。

**記録**: RSA-250（829 ビット）が 2020 年に分解された。要した計算量は約 2700 コア年（CPU コア 1 個を 1 年間回した仕事量 × 2700）。

**推奨鍵長**: 2048 ビット以上（2030 年まで）、長期なら 3072 ビット以上。

## 5. 実装上の落とし穴（ここが実務で最重要）

### (a) 教科書 RSA は使ってはいけない

$`C = M^e \bmod n`$をそのまま使うと、以下の問題がある:

**① 決定的である**: 同じ平文は必ず同じ暗号文になる。
平文の候補が少ない場合（「はい/いいえ」など）、**全部暗号化して比較すれば分かる**。

**② 乗法準同型性**:

$$
E(M_1)\cdot E(M_2) = M_1^e M_2^e = (M_1M_2)^e = E(M_1M_2)
$$

攻撃者は**平文を知らないまま暗号文を操作できる**。
これを使うと、「任意の暗号文を復号してくれるが、狙いの$`C`$だけは断る」相手（選択暗号文攻撃の設定）から$`C`$の平文が引き出せる:
乱数$`r`$を選んで$`C' = C \cdot r^e \bmod n`$を復号してもらうと、返ってくるのは$`M' = (M r^e)^d = M r \bmod n`$。
$`C'`$は$`C`$と無関係に見えるので断られない。あとは$`M = M' \cdot r^{-1} \bmod n`$で$`M`$が求まる（**ブラインディング攻撃**）。

### (b) パディング方式

平文$`M`$をそのまま$`e`$乗せず、乱数や固定の書式を付け足して$`n`$未満の整数に整形してから暗号化する。この整形を**パディング**という。
上の ① ② はどちらも「$`M`$をそのまま使う」ことが原因なので、パディングで潰す。

| 方式 | 用途 | 状態 |
|---|---|---|
| PKCS#1 v1.5 | 暗号化 | **非推奨**（Bleichenbacher 攻撃） |
| **OAEP** | 暗号化 | 推奨 |
| PSS | 署名 | 推奨 |

**OAEP** はランダム性を注入して確率的にし、CCA 安全性を持たせる。

> **Bleichenbacher 攻撃（1998）の教訓.**
> PKCS#1 v1.5 では、復号後にパディング形式が正しいかを検査する。
> サーバが「パディング不正」というエラーを返すと、攻撃者は「自分の作った暗号文が正しい書式に復号されるか」を 1 ビットずつ教えてくれる装置
> （**オラクル**: 中身は見せないが問い合わせには答える相手）としてサーバを使える。
> ② の準同型性で$`C \cdot r^e`$の形の暗号文を大量に送り、書式が合う$`r`$を集めて$`M`$の範囲を絞り込んでいくと、
> 1024 ビット鍵で数十万〜百万回程度の問い合わせで暗号文が復号できてしまう。
>
> [12 章](12_暗号の基礎概念と古典暗号.md#攻撃モデル-安全-を定義するために)で「選択暗号文攻撃は非現実的ではない」と述べた実例がこれである。
> しかも 20 年以上経った 2017 年にも **ROBOT 攻撃**として、
> Facebook や PayPal を含む多数のサーバに同じ脆弱性が残っていることが判明した。

### (c) 素数生成の失敗

**同じ素数を再利用すると即座に破れる**: 2 つの公開鍵$`n_1 = pq_1`$、$`n_2 = pq_2`$が
共通の$`p`$を持つと、$`\gcd(n_1,n_2) = p`$が**ユークリッドの互除法で一瞬で求まる**。

2012 年の大規模調査で、インターネット上の TLS 証明書の**約 0.5%** が
他の証明書と素数を共有しており、秘密鍵が計算可能だった（乱数生成器の初期化不良が原因）。

### (d) サイドチャネル攻撃

- **タイミング攻撃**: 繰り返し二乗法は$`d`$のビットが 1 のときだけ掛け算を 1 回余分に行うので、計算時間を精密に測ると$`d`$のビットが漏れる
  → どのビットでも同じ命令列・同じ時間で動く**定数時間実装**（ビットが 0 でも掛け算を行って結果を捨てる、など）が必須
- **電力解析**: 消費電力の波形から掛け算の有無が読める（SPA = 単純電力解析、1 回の波形を目で読む。DPA = 差分電力解析、多数の波形を統計処理する）
- **フォールト攻撃**: CRT 高速化中にエラーを注入すると$`\gcd`$で$`q`$が求まる（[15 章](15_数論の道具.md)）

## 6. RSA の現在地

> **RSA は「枯れた技術」になりつつある.**
> - 鍵が長い（3072 ビット vs 楕円曲線 256 ビット）
> - 署名・鍵交換とも楕円曲線のほうが高速
> - **量子コンピュータのショアのアルゴリズムで多項式時間で破られる**。ショアのアルゴリズムは「$`a^x \bmod n`$の周期（$`x`$の位数）」を
>   量子フーリエ変換で一気に求める手法で、位数が分かると$`n`$の素因数が$`\gcd`$で出る。十分大きな量子コンピュータが要るのでまだ実用にはなっていない
>   （RSA だけでなく楕円曲線も同様に破られる）
>
> TLS 1.3 では鍵交換から RSA が外され、楕円曲線ディフィー・ヘルマン (ECDHE) が標準になった。
> RSA は署名用途と、既存システムとの互換性のために残っている。
>
> とはいえ **RSA は公開鍵暗号の原理を学ぶ最良の教材**である。
> 「オイラーの定理を使えば、公開鍵で暗号化し秘密鍵で復号できる」という論理は、
> 数論と暗号が結びついた歴史的瞬間そのものである。

## 7. まとめ

| 項目 | 内容 |
|---|---|
| 鍵生成 | $`n=pq`$、$`ed\equiv1 \pmod{\varphi(n)}`$ |
| 暗号化 / 復号 | $`C=M^e`$、$`M=C^d`$ |
| 正しさの根拠 | **オイラーの定理 + フェルマー + CRT**（[15 章](15_数論の道具.md#フェルマー-テスト)） |
| 安全性 | 素因数分解の困難性 |
| 高速化 | 繰り返し二乗法（[01 章](01_合同算術とユークリッドの互除法.md#繰り返し二乗法-巨大なべき乗を現実的な時間で)）、CRT（[15 章](15_数論の道具.md)） |
| **実務の鉄則** | **教科書 RSA を直接使わない。OAEP/PSS を使う** |

→ 次: [17_DHと楕円曲線暗号.md](17_DHと楕円曲線暗号.md)
